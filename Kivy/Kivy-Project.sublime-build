{
	"path": "/usr/bin:/usr/local/bin", // need /usr/bin for readlink (in kivy) and /usr/local/bin for kivy
	"cmd": ["kivy", "${project_path}/main.py"],
	"file_regex": "^[ ]*File \"(...*?)\", line ([0-9]*)",
}
